export enum align {
  start = 'start',
  center = 'center',
  end = 'end'
}

export enum state {
  default = 'DEFAULT',
  hover = 'HOVER',
  focus = 'FOCUS',
  disabled = 'DISABLED'
}

export enum selectState {
	default = 'DEFAULT',
	hover = 'HOVER',
	opened = 'OPENED',
	disabled = 'DISABLED'
}
